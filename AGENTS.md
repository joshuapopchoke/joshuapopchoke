# Agent Guide

- Build command: `npm run build`
- Test command: `npm run test`
- All new questions must conform to the `Question` interface in `C:\Users\range\fiduciary-duty\src\types\question.ts`.
- Compliance engine logic must never change question scoring, and question engine logic must never change SEC scrutiny calculations.
- React components should use PascalCase file names and default to one component per file under `C:\Users\range\fiduciary-duty\src\components`.
- Every new question must include a substantive `explanation` field with at least two full sentences.
